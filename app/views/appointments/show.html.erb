<h1><%= @appointment.datetime %> | <%= @doctor.department %></h1>
<p>Appointments:<br>
  <ul>
    <% @doctor.appointments.each do |appointment| %>
      <li><%= appointment.appointment_datetime %> | <%= link_to appointment.patient, patient_path(appointment.patient) %></li>
    <% end %>
  </ul>
</p>
