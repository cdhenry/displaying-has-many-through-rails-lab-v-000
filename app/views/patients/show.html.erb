<h1><%= @patient.name %></h1>
<p>Appointments:<br>
  <ul>
    <% @patient.appointments.each do |appointment| %>
      <li><%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> | <%= link_to appointment.doctor.name, appointment_path(appointment) %></li>
    <% end %>
  </ul>
</p>
