<h1><%= @patient.name %></h1>
<p>Appointments:<br>
  <ul>
    <% @doctor.appointments.each do |appointment| %>
      <li><%= appointment.datetime %> | <%= link_to appointment.patient, patient_path %></li>
    <% end %>
  </ul>
</p>