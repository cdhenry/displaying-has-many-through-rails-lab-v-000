<h1><%= @patient.name %></h1>
<p>Appointments:<br>
  <ul>
    <% @patient.appointments.each do |appointment| %>
      <li><%= appointment.appointment_datetime %> | <%= link_to appointment.doctor, appointment_path(appointment) %></li>
    <% end %>
  </ul>
</p>
